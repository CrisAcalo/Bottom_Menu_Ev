class AgeResult {
  final int years;
  final int months;
  final int days;

  // Constructor para crear una instancia del resultado.
  // 'required' significa que estos valores deben ser proporcionados siempre.
  AgeResult({
    required this.years,
    required this.months,
    required this.days,
  });
}